---
description: jkpt API 自动化测试用例生成约束（强制以仓库技能为唯一真相源）
globs: testcases/**, yaml/**, common/**, conftest.py, skills/api-test-framework/**, jkpt_api_test/testcases/**, jkpt_api_test/yaml/**, jkpt_api_test/common/**, jkpt_api_test/conftest.py
---

# jkpt 接口测试生成规则

本仓库的接口自动化生成 **必须** 遵循以下约束。技能来源 = 仓库根目录：

- 主文档：[skills/api-test-framework/SKILL.md](mdc:skills/api-test-framework/SKILL.md)
- 方法字典：[skills/api-test-framework/references/methods-reference.md](mdc:skills/api-test-framework/references/methods-reference.md)
- 适配层 Fixture：[skills/api-test-framework/references/conftest-jkpt.md](mdc:skills/api-test-framework/references/conftest-jkpt.md)
- 适配层 YAML：[skills/api-test-framework/references/yaml-conventions.md](mdc:skills/api-test-framework/references/yaml-conventions.md)
- 变更跟踪：[skills/api-test-framework/CHANGELOG.md](mdc:skills/api-test-framework/CHANGELOG.md)

> 同时存在的 Cursor 全局 `~/.cursor/skills/api-test-framework/` 与本仓库**不一致**，**禁止**作为生成依据。

## 必须

- HTTP 请求入口：`from common.requests_util import BaseRequest`
- 普通 REST 统一信封断言：`from common.case_report_util import assert_response`
- `assert_api_result` 仅作公共层底层比较器，普通 testcase 禁止直接调用
- YAML 读取：`from common.yaml_util import read_yaml, write_yaml, clear_yaml, read_expected_msg`
- 正向 YAML：`expected.msg`；负向 YAML：`expected.error_msg`（禁止正向写 `error_msg: "成功"`）
- 日志：`from common.logger_util import sep, key, print_request, print_response`
- 数据驱动：`@pytest.mark.parametrize("case", …)`，**不传 `ids=`**，不要 `@allure.title` 用 YAML `name`
- 用例模式：**模式 A**（无状态，多接口仍拆类）或 **模式 B′**（文件内多类 + `extract.yaml`）
- 多接口文件：**一类一 HTTP 接口**；类名 `Test<前缀><两位序号>…`（如 `TestEn01`）；Helpers 不以 `Test` 开头
- 单接口文件（登录 / 协议探测）允许一类到底
- jsonpath 风格：`_jsonpath_parse = jsonpath.jsonpath`（函数式 API）
- 鉴权头：注入 `auth_headers` fixture；`no_auth: true` 用例**方法体内** strip Authorization
- 协议用例：注入 `bd_client` + `bd_test_terminal`，断言 `result.success`

## 禁止

- `from api_test_framework.runner import run_case`（该包已删除）
- `from api_test_framework.<anything> import ...`（仅 common/ 进入用例层）
- `pytest_plugins = ["api_test_framework.pytest_plugin"]`
- 模式 C（纯 YAML + `run_case`）
- 全局技能的 YAML 结构：`version: "1.0"`、`assertions: [{type: ..., expected: ...}]`、`api-test-generate` CLI
- YAML 中写可执行表达式（`!python`、`eval` 等）
- 在用例中硬编码生产 URL、明文密码、真实手机号 / 身份证
- 在 `conftest.py` 写 `extract.yaml`（清理职责单独，写入由 testcase 完成）
- 模式 B 单类 + `test_data[:N]` 切片（Suites 会摊平）
- 中文 `parametrize ids=`、`@allure.title(case["name"])`
- 有 extract 链的文件按 class 做 pytest-xdist
- 清理只挂在最后一个 Test 类（`-k` 跳过该类会漏删）

## 命名与文件

- testcase：`testcases/test_<controller>.py`
- YAML：`yaml/test_<controller>.yaml`，顶层 key 以 `_cases` 结尾
- YAML 占位符：`{{变量名}}`；jkpt 特例 `{{bd_test_terminal}}`、`{{one_id}}`/`{{two_id}}`/`{{three_id}}`

## 决策树（生成时）

1. 接口无依赖 → 模式 A（多接口仍一类一接口）
2. 接口有 CRUD 链路 → 模式 B′（文件内多类 + extract.yaml）；不要模式 B 类变量
3. 二进制协议 → 协议层 + `bd_client.send_*`
4. 普通 JSON REST → `assert_response(...)`；协议/导出保留专用断言
5. 失败上下文 → `BaseRequest` 已附请求/响应，Hook 仅兜底，不重复附加
6. YAML `name` → 日志/附件；Suites 叶子用默认参数下标

## 生成后自检

- 仅引用 `common.*` 与 `pytest`、`jsonpath`、标准库
- YAML 顶层 key 与 `parametrize` **一类一 key**
- 类名带补零序号；collect 不是单类摊平整个 controller
- 正向 `expected.msg`、负向 `expected.error_msg`
- `no_auth` 路径与正向路径在同一方法内分支
- 协议用例不出现 `auth_headers`
- 普通 REST 没有直接 import/call `assert_api_result`，没有自行重复解析 `$.code/$.msg`
- 没有 `run_case` / `pytest_plugin` / `version: "1.0"`

## 公共能力提取规则

- ≥2 个 testcase 重复 ≥5 行同样逻辑 → 抽到 `common/<topic>_util.py`
- 同步更新 [methods-reference.md](mdc:skills/api-test-framework/references/methods-reference.md) + [CHANGELOG.md](mdc:skills/api-test-framework/CHANGELOG.md)
- 详细流程：[CONTRIBUTING.md](mdc:skills/api-test-framework/CONTRIBUTING.md)
